@page
@model TOHPO.Pages.Configuracion.Motivo_Recordatorio.IndexModel
@{
    ViewData["Title"] = "Gestión de Motivos de Recordatorio";
}

<form method="post">
    @Html.AntiForgeryToken()
</form>

<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <!-- ENCABEZADO -->
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3 class="card-title mb-0">
                        @ViewData["Title"]
                    </h3>
                    <div class="card-tools d-flex gap-2">
                        <a asp-page="/Index" class="btn btn-light">
                            <i class="bi bi-arrow-left"></i> Regresar
                        </a>
                        <a asp-page="/Configuracion/Motivo_Recordatorio/Upsert" class="text-center btn btn-primary">
                            <i class="bi bi-plus-circle"></i> Crear Motivo
                        </a>
                    </div>
                </div>

                <!-- CONTENIDO PRINCIPAL: TABLA -->
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="tablaMotivos" class="table table-striped table-bordered table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th class="text-center">Id</th>
                                    <th class="text-center">Descripción</th>
                                    <th class="text-center">Acciones</th>
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/Configuracion/motivos.js" asp-append-version="true"></script>
}