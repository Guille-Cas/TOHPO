@page "{id?}"
@using TOHPO.Models
@model TOHPO.Pages.Configuracion.Categorias.UpsertModel
@{
    var isEdit = Model.Categoria != null && Model.Categoria.Id > 0;
    ViewData["Title"] = isEdit ? "Editar Categoría" : "Crear Categoría";
}

<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card">

                <!-- ENCABEZADO -->
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3 class="card-title mb-0">
                        @ViewData["Title"]
                    </h3>
                    <div class="card-tools d-flex gap-2">
                        <a asp-page="/Configuracion/Categorias/Index" class="btn btn-light">
                            <i class="bi bi-arrow-left"></i> Regresar
                        </a>
                    </div>
                </div>

                <!-- DESCRIPCIÓN (ALERT) -->
                <div class="card-body border-bottom">
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                        @(isEdit
                                                ? "Actualice la información de la categoría y guarde los cambios para mantener sus datos al día."
                                                : "Complete la información de la nueva categoría y guarde el registro para que quede disponible en el sistema.")

                    </div>
                </div>

                <!-- CONTENIDO PRINCIPAL: FORMULARIO -->
                <div class="card-body">
                    <form method="post">
                        @Html.AntiForgeryToken()

                        @if (isEdit)
                        {
                            <input type="hidden" asp-for="Categoria.Id" />
                        }

                        <div class="mb-3">
                            <label asp-for="Categoria.Descripcion" class="form-label"></label>
                            <input asp-for="Categoria.Descripcion" class="form-control" />
                            <span asp-validation-for="Categoria.Descripcion" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-end gap-2 mt-3">
                            <button type="submit" class="btn btn-primary">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
